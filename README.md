# Express.js Fundamentals 🚀

This repository demonstrates the core concepts of **Express.js**, the most popular web framework for Node.js. It covers middleware creation, routing, and structuring a basic Express application.

## 📂 Project Structure

Here is an overview of the files and their purposes:

| File | Description |
|------|-------------|
| **`index.js`** | The entry point of the application. Starts the server. |
| **`app-module.js`** | Modularized application logic (separation of concerns). |
| **`middleware.js`** | Examples of built-in and third-party middleware usage. |
| **`custom-middleware.js`** | Implementation of custom middleware functions (logging, auth simulation, etc.). |
| **`routes-example.js`** | Handling GET, POST, PUT, DELETE requests and route parameters. |

## 🛠️ Getting Started

### Prerequisites
* [Node.js](https://nodejs.org/) installed on your machine.

### Installation

1.  Clone the repository:
    ```bash
    git clone [https://github.com/m2rtyaman/expressjs-fundamentals.git](https://github.com/m2rtyaman/expressjs-fundamentals.git)
    ```

2.  Navigate to the project folder:
    ```bash
    cd expressjs-fundamentals
    ```

3.  Install dependencies:
    ```bash
    npm install
    ```

## 🚀 Usage

You can start the server using Node:

```bash
node index.js
